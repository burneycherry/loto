# 変更履歴

バージョン番号は `v<メジャー>.<マイナー2桁>` 形式です。

- **v1.00** … 初版
- **v1.10** … みずほからの自動取得に対応
- **v1.11** … 取得先URLの修正と解析テストの追加
- **v1.12** … 過去回を月送りで取得
- **v1.13** … 取得元の指定を整理、アプリ本体のテストを追加
- **v1.14** … 取得時の要求ヘッダを修正
- **v1.20** … ページのコピーをそのまま貼り付け可能に
- **v1.30** … 一覧表形式に対応、日付の読み違いを修正、画面を整理
- **v1.40** … 予想を頻度帯による方式に変更
- **v1.41** … 帯の集計から自分自身を除外
- **v1.50** … 1口ずつの作り直しに対応（現在）
- **v1.01, v1.02 …** … 不具合修正・小さな改善（カウントアップ）
- **v1.10, v1.20 …** … 機能追加
- **v2.00 …** … 仕様変更・作り直し

更新するときは次の3か所を必ず同じ値に揃えてください。

1. `VERSION` ファイル
2. `index.html` の `APP_VERSION`（画面右上のバッジと「情報」タブの履歴表）
3. `sw.js` の `APP_VERSION`（キャッシュ名が変わり、PWAが新版を取り込みます）

そのうえで `git tag v1.01` のようにタグを打ちます。


## v1.50 (2026-09-22)

### 追加

- 生成した予想を**1口ずつ作り直せる**ようにしました。各口の「この口だけ作り直す」を押すと、
  他の口はそのまま残り、その口だけが選び直されます。他の口と同じ組み合わせにはなりません。
  作り直したのに同じ内容では分かりにくいため、いまの組み合わせも避けます
  （選べる組み合わせが少なく避けきれない場合だけ許します）。
  帯の個数を自分で決めている場合は、作り直してもその指定を守ります。

### 確認したこと

- ロト7でも頻度帯は3つが妥当でした。公平な抽せんを仮定したシミュレーションでの、
  帯の組み合わせのうち「1回しか出ていないもの」の割合は次のとおりです。
  実績から抽せんする仕組みのため、この割合が高いほどノイズを覚えているだけになります。

  | 対象回数 | 帯の数 | ロト6 | ロト7 |
  | - | - | - | - |
  | 24回 | 3本 | 42.6% | 46.7% |
  | 24回 | 4本 | 58.0% | 66.4% |
  | 24回 | 5本 | 68.0% | 73.0% |
  | 100回 | 3本 | 21.9% | 23.9% |

  ロト7は本数字が7個あるぶん組み合わせの種類が増えるため、帯を増やす余地はロト6より小さいです。


## v1.41 (2026-09-22)

### 修正（重要）

- **頻度帯の組み合わせを数えるとき、その回自身を対象期間に含めていたのを修正しました。**
  当せん番号は自分自身で出現回数を1増やすため、その6個は必ず1段階上の帯へ押し上げられ、
  加熱帯がちょうど1個ぶん水増しされていました。
  公平な抽せんを仮定したシミュレーション（ロト6・対象24回・3000回試行）での確認結果は次のとおりです。

  | 数え方 | 加熱 | 標準 | 低頻度 |
  | - | - | - | - |
  | その回自身を含む（誤り） | 2.43個 | 2.68個 | 0.90個 |
  | その回より前だけ（正しい） | 1.44個 | 2.60個 | 1.96個 |

  各回の帯は、その回より前のデータだけで決めるようにしました。
  これから買う回はまだ抽せんされていないので、予想するときの条件とも一致します。
- 履歴が足りない場合（前のデータが10回未満）は対象期間そのもので代用し、
  加熱帯が多めに出る旨を画面に表示します。データが増えると自動で正しい数え方に切り替わります。

### 追加

- 元に戻ってしまったときに気づけるよう、テストに独立した実装を書いて結果を突き合わせています。
  作り物の並び40回ぶんで、アプリの集計とテスト側の集計が一致することを確認します（82項目）。


## v1.40 (2026-09-22)

予想を「頻度帯」による方式に作り替えました。

### 変更

- 数字を出現回数で3つの帯に分けるようにしました。対象期間の本数字の出現回数を、
  理論期待値 ± 標準偏差の半分を境目として、加熱帯・標準帯・低頻度帯に分類します。
- **予想は「帯ごとに何個選ぶか」で組み立てます。** 当せん番号は加熱帯だけで揃うわけではなく
  毎回いくつかの帯が混ざるため、対象期間の各回が「加熱何個・標準何個・低頻度何個」で
  構成されていたかを数え、その実績から構成を抽せんします。個数を自分で決めることもできます。
- 帯の中からは等確率で選びます。帯に分けた時点で出現回数の多い少ないが反映されているためです。
- 以前の「重み付けモード（頻度どおり・ホット・コールド・重み付けなし）」は廃止しました。
  数字ごとの重みで選ぶと加熱帯ばかりが集まりやすく、実際の当せん番号の構成と合わないためです。
- 貼り付けの結果表示を「3件読込 ／ 1件新規追加 ／ 2件取込済（重複）」の形に変えました。

### 追加

- 「出現率」タブに頻度帯の内訳（境目・個数・どの数字がどの帯か）を表示します。
- 「条件確率」タブに頻度帯の組み合わせ（各回が何個ずつだったか）の集計を表示します。
- 「予想」タブに、いまの帯の状況と、構成を決める表を表示します。

### 削除

- ダミーデータ生成。
- 「24回分以上あると分析が安定します」「既定24回」といった誤解を招く表現。
  集計する回数を増やしても次回の当たりやすさは変わらないことを、代わりに明記しました。


## v1.30 (2026-09-22)

### 修正（重要）

- **スラッシュ区切りの日付を当せん番号として読み違える不具合を直しました。**
  ページのコピーを解析する処理が「2026年9月21日」形式しか日付と認識しておらず、
  「2026/9/21」形式の一覧表では日付の 9 と 21 を本数字として取り込んでいました
  （件数は合うのに中身がずれる状態でした）。日付の判定をスラッシュ・ドット・ハイフン区切りにも広げ、
  回号の直後200文字以内だけを日付として探すようにしました。
  見出しも日付も無いまとまりは取り込まないようにしています。

### 追加

- 回ごとに1行の一覧表（`第2139回 2026/9/21 03 06 23 34 36 43 29 1 4億5,134万円 0円` のような形）
  の貼り付けに対応しました。タブ区切り・スペース区切り・セルごとに改行される形のいずれも読めます。
  賞金額・口数・キャリーオーバーの数字は取り込みません。
- 予想タブに「この予想の作り方」を追加しました。重みの計算式、②の傾向へ合わせる5つの条件、
  条件を外す順番、そして当せん確率は上がらないことを明記しています。

### 変更

- **取り込みは常に追加だけになりました。** 回号ごとに当せん番号は決まっているため、
  置き換えは不要と判断しました。同じ回は重複しません。入れ直すときは「全消去」を使います。
- 予想の重み付けモードの名前を実態に合わせました。「バランス」は
  ホットとコールドを混ぜる意味ではなく出現回数に比例させることなので、
  「頻度どおり（出現回数に比例）」に改めました。

### 削除

- 保存済みJSONの読み込み（取り込んだデータは端末に残るため不要）。
- 手入力での1回追加（貼り付けで足りるため）。


## v1.20 (2026-09-22)

みずほのページをブラウザでコピーして、そのまま貼り付けられるようにしました。

### 追加

- 「抽せん数字一覧表」をSafariなどでページごとコピーして貼り付けると、
  その月の全回をまとめて取り込めます。1回分が複数行にまたがる形式に対応しました。
  見出しの「2026年9月分　（第2134回〜第2139回）」の回号や、賞金額・口数は取り込みません。
- 取り込みは「1行1回」と「ページをコピーした形式」の両方を試し、読めた件数が多い方を採用します。

### 変更

- GitHub Actions からみずほへ接続すると HTTP 403 で拒否されることが分かりました。
  独自の User-Agent でも、一般的なブラウザと同じヘッダ一式でも結果は同じで、
  実行元のIPで拒否されていると考えられます。ブロックを回避する方向には進みません。
- そのためワークフローの取得ステップは `push` では実行しません（毎回失敗通知が飛ぶため）。
  テストは従来どおり毎回実行します。手動実行と定期実行のときだけ取得を試みます。
- アプリの「保存済みデータを読み込む」の説明を実態に合わせ、
  `data/*.json` は手元で `tools/fetch_mizuho.js` を実行したときに作られる旨を明記しました。


## v1.14 (2026-09-22)

### 修正

- みずほへの接続が HTTP 403 で拒否されたため、要求ヘッダを一般的なブラウザと同じ内容に変更しました
  （`User-Agent`、`Accept`、`Accept-Language`、`Referer` など）。`LOTO_UA` で差し替えられます。
- 403 のときは「User-Agent が弾かれているか、実行元のIPが拒否されている可能性がある」旨を
  エラーメッセージに出すようにしました。


## v1.13 (2026-09-22)

取得元の指定が分かりにくく、みずほのURLを入れてしまう事故が起きたため作り直しました。

### 修正

- アプリの取得元は既定で「このページと同じ場所の `data/`」を見るようにしました。
  GitHub Pages で公開していれば、URL欄は空欄のままで動きます。
- 取得元にみずほのURLが入っている場合は、通信せずに理由を表示します。
  以前のバージョンで保存されていた場合は起動時に自動で既定に戻します。
- 「取得元を既定に戻す」ボタンを追加。
- 取得に失敗したときは、試したURLと「data/*.json がまだ作られていない可能性」を表示します。
- ワークフローに `push` トリガーを追加。`schedule` と `workflow_dispatch` は
  既定ブランチのワークフローしか動かないため、作業ブランチでは取得が始まりませんでした。
  コミット先も実行中のブランチを明示するようにしました。

### 追加

- `tools/test_app.js` … アプリ本体のテスト（34項目）。ブラウザもネットワークも使わず、
  `index.html` の `<script>` を隔離環境で実行して描画結果を検査します。
  統計の集計値、予想の生成、貼り付け取り込み、自動取得の挙動、
  3か所のバージョンの一致、CSSの決まりごとを確認します。
- `CLAUDE.md` … 作業上の決まりごと（ブランチと公開、コーディング規約、テスト、
  バージョン管理、データの扱い、取得先への配慮）。


## v1.12 (2026-09-22)

過去回の取得方法を、実際のURL仕様に合わせて月送り方式に変更しました。

### 変更

- みずほの「抽せん数字一覧表」は月ごとにページが分かれ、
  `index.html?year=2026&month=8` の形式で過去の月を参照できます。
  リンクを辿る方式をやめ、当月ページを起点に年月を1か月ずつ遡ってURLを組み立てる方式にしました。
- 新しい回が見つからない月が2回続いたら打ち切ります。
  既存の `data/*.json` を読んでから取得するため、初回のみ過去をまとめて取り込み、
  以降の定期実行は当月ページの1リクエストで済みます。
- 遡る上限は既定30か月（`LOTO_MONTHS`）。ロト6は2000年、ロト7は2013年より前には遡りません。

### 追加

- `LOTO_SLEEP` … リクエスト間隔のミリ秒（既定1200）。テスト用で、むやみに小さくしないこと。
- テストを30項目に拡充。月別URLの組み立て（実URLとの一致）、年またぎの前月計算、
  月を遡る取得、空の月が2回続いたときの打ち切り、上限到達時に当月だけ取得することを確認します。


## v1.11 (2026-09-22)

みずほ銀行の実際のページ構成に合わせて取得処理を修正しました。

### 修正

- 取得先URLを実際のページに修正。
  ロト6: `https://www.mizuhobank.co.jp/takarakuji/check/loto/loto6/index.html`
  ロト7: `https://www.mizuhobank.co.jp/takarakuji/check/loto/loto7/index.html`
- 「抽せん数字一覧表」は1か月分ずつの掲載で、見出しに
  「2026年9月分　（第2134回〜第2139回）」のように回号が入ります。
  この見出しの回号を抽せん結果として取り込まないことをテストで確認しました。
- 過去回のリンク探索を作り直し。同じディレクトリ配下のページと backnumber を含むリンクを候補にし、
  バックナンバー・月別アーカイブらしいものを優先して辿ります（他のロト種別や外部リンクは除外）。
  中身が空のページは訪問数に数えず、既定で8ページまで辿ります。

### 追加

- `tools/test_parse.js` … 解析処理のテスト（ネットワーク不要、20項目）。
  実ページの構成を再現した `tools/testdata/*.html` を使い、回号・抽せん日・本数字・
  ボーナス数字の抽出、賞金額や口数を拾わないこと、見出しの回号を除外すること、
  Shift_JIS判別、全角数字の変換、リンクの優先順位、検証の失敗判定を確認します。
- ワークフローは取得前にこのテストを実行します。
- `LOTO_URLS` … 取得するURLをカンマ区切りで直接指定できる環境変数（巡回せずその一覧だけを読む）。


## v1.10 (2026-09-22)

みずほ銀行のサイトからの当せん番号自動取得に対応。

### 追加

- `tools/fetch_mizuho.js` … みずほ銀行のロト6・ロト7ページから当せん番号を取得して
  `data/loto6.json` `data/loto7.json` に保存するNodeスクリプト（外部パッケージ不要）。
  Shift_JIS・EUC・UTF-8の自動判別、「本数字」「ボーナス数字」見出しを手がかりにした抽出、
  見出しが無い場合のフォールバック、賞金欄の数字の除外、バックナンバーページの巡回、
  既存JSONとのマージ、取得内容の検証（不正なら終了コード1）に対応。
- `.github/workflows/fetch-loto.yml` … 上記を毎日21時(JST)に実行し、差分があれば自動コミット。
  手動実行（workflow_dispatch）も可能。
- アプリの「データ」タブに「みずほのデータを自動取得」を追加。
  保存済みJSONを取得して既存データにマージします。取得元URLは変更可能で、
  JSONのほかCSVやテキスト形式のURLもそのまま読み込めます。
  「起動時に自動で取得する」を有効にすると、アプリ起動時に自動で最新データを取り込みます。

### 補足

- ブラウザからみずほ銀行のページを直接読み込むことはCORS制限でできないため、
  取得はGitHub Actions側で行い、アプリは生成済みJSONを読む構成にしています。
- 取得先に負荷をかけないよう、リクエスト間隔を空けて1日1回の実行にとどめています。

### 変更

- Service Worker のキャッシュ対象を同一オリジンのGETのみに限定（取得したデータが古いまま残らないように）。


## v1.00 (2026-09-22)

初版リリース。

### 追加

- ロト6（本数字6個＋ボーナス1個／1〜43）とロト7（本数字7個＋ボーナス2個／1〜37）の切り替え。
- 当せん番号の取り込み。1行1回の貼り付けに対応し、`第1900回 2024年5月2日 03 11 19 24 35 41 (07)`、
  `1900,2024/05/02,3,11,19,24,35,41,7`、数字のみの行などを自動判別。全角数字も可。
  範囲外・重複の数字を含む行は理由付きで読み飛ばし。
- 手入力フォーム、1回単位の削除、JSON／CSV書き出し、動作確認用ダミーデータ生成。
- データはブラウザの localStorage に保存（端末内のみ）。
- **① 出現率タブ**: 最新回から指定回数分（既定24回）について、本数字／ボーナス／合算の出現回数を集計。
  「出現回数4回 : 3個」のような出現回数別のまとめを全件表示し、数字別一覧・出現率・ヒートマップ表示も併記。
- **② 条件確率タブ**: 引っ張り数字（前回と同じ番号）の個数分布、連続数字（連続なし／2連続1組／2連続2組以上／3連続／4連続以上）、
  末尾同一数字（なし／2個一致1組／2個一致2組以上／3個一致以上）、合計値レンジ、奇数偶数の分布。回別の明細表も表示。
- **◎ 予想タブ**: ①の出現回数から重み（バランス／ホット／コールド／均等）を作り、
  ②で得た分布に沿うように引っ張り個数・連続・末尾一致・合計値・奇数偶数を満たす組み合わせを生成（最大10口）。
- iOS Safari / PWA 向けの対応（セーフエリア、ホーム画面追加、ダークモード、オフライン動作）。

### 注意

- 抽せんは毎回独立した事象です。本アプリの統計・予想は過去傾向の可視化であり、当せんを保証しません。
