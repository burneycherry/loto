# CLAUDE.md

このリポジトリで作業するときの決まりごと。変更する前にここを読むこと。

## このリポジトリ

ロト6・ロト7の当せん番号を集計して予想を作るWebアプリ。`index.html` 単体で動く。
当せん番号は、利用者が一覧ページをコピーしてアプリに貼り付けることで取り込む。

## ブランチと公開

- **開発は `main` から新しいブランチを切って行う。** `main` へ直接 push しない。
  ブランチ名は任意（`claude/…` でも `feature/…` でもよい）。
- 一区切りついたらテストを通して push し、`main` へ早送りマージして push する。
  `git push -u origin <ブランチ名>` で push する。タグの push は環境によって
  拒否されることがある（その場合はローカルにタグだけ残す）。
- **公開（GitHub Pages）の配信元に push した時点で、公開物が入れ替わる。**
  壊れた状態を push しない（後述のテストを必ず通してから push する）。
  配信元は Settings → Pages で確認する。`main` にしておくと、
  マージした時点で公開されるので分かりやすい。
- v1.51 までの開発はブランチ `claude/nifty-mccarthy-gzqj7n` で行った。
  その時点の Pages の配信元はこのブランチだった。
- コミットメッセージ・コメント・画面の文言はすべて日本語。

## アプリの構造

`index.html` 1ファイル（約1700行）に、CSS と JavaScript を同梱している。
JavaScript は1つの即時実行関数の中にあり、外に出しているものは無い。
おおよそ次の順で並んでいる。

| 範囲 | 中身 |
| - | - |
| 設定と小道具 | `APP_VERSION`、`GAMES`（ロト6/7の定義）、`state`、`save` / `load`（localStorage） |
| データ取り込み | `parseDraws`（1行1回）、`parseBlocks`（ページをコピーした形式）、`importText`、`mergeInto` |
| 集計 | `calcFreq`（出現回数）、`calcConditions`（引っ張り・連続・末尾・合計・奇数） |
| 頻度帯 | `bandInfo`（帯の判定）、`bandComposition`（帯の組み合わせ）、`pickByBands` |
| 予想 | `predictContext`（材料をまとめる）、`generateOne`（1口）、`generatePicks`、`regenOne`（作り直し） |
| 画面 | `viewData` / `viewFreq` / `viewCond` / `viewPred` / `viewInfo`、`render`（毎回まるごと描き直す） |

画面は状態が変わるたびに `render()` で作り直す。イベントは `document` 上の
1つのハンドラに集約し、`data-act`（動作）と `data-set`（設定の変更）で振り分ける。

`tools/test_app.js` は `index.html` の `<script>` を取り出し、DOM・localStorage・fetch を
差し替えた関数として実行して、描画結果の文字列を検査する。
新しいセッションでまず動かすとよい。

## コーディングの決まり

利用者は主に iOS Safari とホーム画面に追加したPWAで使う。Androidでも動くようにする。

- **CSSカスタムプロパティを使わない。** ハイフン2つで始まる変数名と `var()` による参照は禁止。
  コピペしたときに記号が変換されて壊れるため。色は実際の値を直接書き、
  ダークモードは `@media (prefers-color-scheme: dark)` で同じ値を上書きする。
- **ハイフンが2つ続く並びをファイル内に書かない。** 同じ理由。
  HTMLコメントの記法も使えない。JavaScriptの減算代入は `i = i - 1` と書く。
  コマンドラインのオプションはハイフン1つにするか、位置引数か環境変数にする。
- SyntaxError に注意する。`tools/test_app.js` は `index.html` の `<script>` の中身を
  関数として組み立てて実行するので、文法エラーがあればテストが失敗する。
- 書き方は ES5 相当に統一する（`var` を使い、アロー関数・テンプレートリテラル・`const`/`let` は使わない）。
  古いiOS端末で動かなくなるのを避けるため。
- `index.html` は単一ファイルのまま。外部ライブラリやCDNを読み込まない。
- 画面の余白は `env(safe-area-inset-*)` で確保する。

## テスト

push の前に**必ず通す**。失敗したら push しない。

```
node tools/test_app.js
```

- `tools/test_app.js` … `index.html` のアプリ本体。統計の集計値、予想の生成、貼り付け取り込み、
  頻度帯の分類と構成、1口ずつの作り直し、バージョンの一致、CSSの決まりごとを検査する。
  ブラウザもネットワークも使わない。
- 機能を足したらテストも足す。**テストを消したり無効にして通すのは禁止。**
- GitHub Actions も push のたびにこれを実行する。

## バージョン管理

`v<メジャー>.<マイナー2桁>`。不具合修正や小さな改善で +0.01、機能追加で +0.10、作り直しで +1.00。

更新するときは次の3か所を**同じ値**にする。ズレていると `tools/test_app.js` が失敗する。

1. `VERSION`
2. `index.html` の `APP_VERSION`（画面右上のバッジ）
3. `sw.js` の `APP_VERSION`（キャッシュ名が変わり、PWAが新版を取り込む）

そのうえで `CHANGELOG.md` に追記し、「情報」タブの履歴表にも1行足す。`git tag v<版>` を打つ。

## データの扱い

- **当せん番号を作り出さない。** 画面に入れてよい当せん番号は、利用者が貼り付けたものだけ。
  テストの中では、解析や集計の動作を確かめるための作り物の並びを使ってよい。
  ただし当せん番号として提示しないこと（テストファイル内にその旨を書く）。
- ダミーデータを作る機能は v1.40 で削除した。作り直さない。
  古いデータに `demo: true` が残っている場合の警告表示だけ残してある。

## 分かっていること（同じ失敗を繰り返さないために）

- **当せん番号の自動取得は実現できなかった。** v1.10 から v1.20 にかけて、みずほ銀行の
  「抽せん数字一覧表」を GitHub Actions で取得する仕組みを作ったが、
  みずほ側が **HTTP 403 で接続を拒否**した。独自の User-Agent でも、一般的なブラウザと
  同じヘッダ一式でも結果は同じで、実行元のIPによる拒否と考えられる。
  **ブロックを回避する方向（IPを変える、偽装を重ねる、第三者の串を通す）には進まない。**
  相手が機械的アクセスを断っているなら、その意思を尊重する。
  取得の仕組みは v1.51 で削除した。**作り直さない。** 経緯は `CHANGELOG.md` の
  v1.10 / v1.14 / v1.20 / v1.51 の項にあり、コードはタグ `v1.50` の時点に残っている。
  なお、ブラウザからみずほのページを直接読むことは CORS 制限でもできない。
- 当せん番号の取り込みは、一覧ページをブラウザでコピーしてアプリに貼り付ける方法だけ。
  解析は「1行1回」と「ページをコピーした形式」の両方を試し、読めた件数が多い方を採用する。
- **件数が合っていても中身が正しいとは限らない。** v1.30 で、スラッシュ区切りの日付
  （2026/9/21）を当せん番号として取り込む不具合が見つかった。件数は正しく出ていたため、
  値そのものを実物と突き合わせるまで気づけなかった。解析を変えたときは、
  必ず実際の値と1件ずつ照合すること。
- 取り込みは常に追加のみ。回号ごとに当せん番号は決まっているので置き換えは不要。
  結果は「3件読込 ／ 1件新規追加 ／ 2件取込済（重複）」の形で伝える。
- **「たくさん集めると分析が安定する」といった書き方をしない。** 抽せんは独立事象なので、
  集計回数を増やしても次回の当たりやすさは変わらない。画面では言えることだけを書く。
- 予想は頻度帯（加熱帯・標準帯・低頻度帯）ごとの個数で組み立てる。
  数字ごとの重みで選ぶと加熱帯ばかりが集まり、実際の当せん番号の構成と合わなくなる。
  帯の判定は本数字の出現回数のみ（ボーナスは使わない）、帯の中は等確率。
- **帯の組み合わせを数えるときは、その回自身を対象期間に入れない。** 当せん番号は
  自分で出現回数を1増やすため、含めると加熱帯がちょうど1個ぶん水増しされる
  （シミュレーションで 2.43個 対 1.44個 と確認済み）。各回の帯はその回より前の
  データだけで決める。予想するときの条件とも一致する。
- **帯の組み合わせを数えるときは、帯の判定に使う期間の長さもそろえる。** v1.52 まで、
  古い標本ほど短い期間で帯を決めていた。期間が短いと帯の母数そのものが変わり
  （ロト6でランダムを仮定すると、10回窓の加熱帯は17.9個・1回あたり2.50個、
  200回窓では12.6個・1.75個）、実績が加熱帯寄りに出る。期間がそろう標本は
  保有Tと対象nに対して `min(n, T - n)` 件なので、窓の長さと標本の数の両方が
  そろうには **保有が対象回数の2倍**（12回なら24件、24回なら48件）必要。
  足りないときは期間を保有の半分にそろえ直す（そこで標本が一番多く取れる）。
  ただし縮めても標本が増えないなら縮めず、足りないことを画面に出す。
  「全件」は対象期間が保有データ全体なので、そろう回は常に0件になる。
- **帯は3つのまま。** 対象24回では、4本にすると帯の組み合わせの6割前後が「1回だけの実績」になり、
  実績から抽せんする仕組みがノイズを覚えるだけになる（ロト6で58%、ロト7で66%）。
  ロト7は本数字が7個あるぶん組み合わせが増えるので、増やす余地はロト6より小さい。
  対象100回まで広げれば4本も検討できる。

## 確認したいことがあるとき

この環境からは外部ネットワークに出られない。実際のページの見た目や構成を知りたいときは、
画面の画像かURLを利用者に出してもらう。推測で実装して「たぶん動く」と言わない。
